name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    permissions:
      # generated-by-copilot: Required to checkout repository code
      contents: read

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install root dependencies
      run: npm ci

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Run backend tests
      run: npm run test:backend

    - name: Build frontend
      working-directory: frontend
      run: npm run build

    - name: Run E2E tests
      run: |
        npm run test:frontend